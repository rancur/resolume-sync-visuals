# API Keys for video generation
# Use `op run --env-file=.env -- rsv generate ...` to inject from 1Password

# OpenAI (DALL-E 3 for image generation)
OPENAI_API_KEY=op://OpenClaw/OpenClaw Secret - OPENAI_API_KEY/password

# Replicate (optional - for Flux/SDXL image generation)
# REPLICATE_API_TOKEN=op://OpenClaw/OpenClaw Secret - REPLICATE_API_TOKEN/password
